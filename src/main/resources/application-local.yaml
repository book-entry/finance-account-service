server:
  port: 8081
  servlet:
    context-path: /account

spring:
  application:
    name: finance-account-service
  datasource:
    url: jdbc:mysql://47.84.7.14:30991/finance_account
    username: account_user
    password: Puy4v-#~pa1@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        format_sql: false
        jdbc:
          time_zone: UTC

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
